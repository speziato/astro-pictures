{
  "name": "@astro-pictures/batch",
  "version": "0.0.1",
  "main": "./dist/index.js",
  "type": "module",
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "npm run clean && tsc",
    "start:dev": "node --no-warnings=ExperimentalWarning --loader ts-node/esm src/index.ts",
    "start": "node dist/index.js",
    "test": "echo \"Tests not implemented yet\" && exit 1",
    "lint": "eslint --ext .ts ./src --fix",
    "format": "prettier . --ignore-path ../.gitignore --write \"**/*.+(ts|json)\""
  },
  "author": "",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "@astro-pictures/utils": "^0.0.1",
    "dotenv": "^16.4.7",
    "minio": "^8.0.5",
    "node-html-parser": "^7.0.1",
    "node-schedule": "^2.1.1",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    
  }
}
